# Docs

Oxpecker is currently an early-stage Zig verification library centered on explicit-state model checking.

The docs describe the library interface, development commands, and example organization. The API is still expected to change as the checker becomes more useful for review and agent-generated code workflows.


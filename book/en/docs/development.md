# Development Commands

Common commands:

```sh
bun run dev
bun run build
zig build
zig build test
```

Use the site build to verify documentation changes, and use Zig build/test commands to verify checker changes.


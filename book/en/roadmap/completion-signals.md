# Completion Signals

Oxpecker roadmap items are complete only when tool behavior exists and can be checked.

Every completed capability should have:

- a runnable Zig example,
- a failing case,
- a passing case,
- a reproducible command,
- enough trace or report output for review,
- and an explanation of what the result does not prove.


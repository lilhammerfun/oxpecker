# Toolchain Integration

Toolchain Integration makes Oxpecker useful in local development, CI, review, and coding-agent workflows.

The desired workflow is that an agent changes stateful code, Oxpecker runs the related specs, and review receives concrete evidence about properties, bounds, traces, and unsupported areas.


# Model Expression

Model expression is the ability to write models clearly without losing the discipline of state-transition thinking.

As examples grow, raw action functions become repetitive. In the booklet, these helpers are introduced only when they make the FV idea easier to understand: guards, updates, bounded domains, and reusable model patterns should make specifications easier to review, not merely shorter.
